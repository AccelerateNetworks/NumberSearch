@model IEnumerable<NumberSearch.DataAccess.OwnedPhoneNumber>
@{
    ViewData["Title"] = "Owned Phone Numbers";
}
<br />
<h1 class="display-4">Owned Phone Numbers</h1>
<br />
<div class="pb-3">
    <ul class="nav nav-pills nav-fill nav-justified">
        <li class="nav-item">
            <a class="nav-link" asp-controller="Home" asp-action="Numbers">Numbers</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" asp-controller="Home" asp-action="NumberOrders">Number Orders</a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" asp-controller="Home" asp-action="OwnedNumbers">Owned Numbers</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" asp-controller="Home" asp-action="Emails">Sent Emails</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" asp-controller="Home" asp-action="PortRequests">Port Requests</a>
        </li>
    </ul>
</div>
<div class="table-responsive">
    <table class="table table-striped table-borderless table-hover" id="table">
        <thead>
            <tr>
                <th scope="col">
                    Number
                </th>
                <th>Status</th>
                <th scope="col">
                    Number Information
                </th>
                <th>
                    Notes
                </th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var number in Model.Where(x => x.IngestedFrom != "Test"))
                {
                    var ingestedBadge = number.IngestedFrom;
                    switch (number.IngestedFrom)
                    {
                        case "FirstPointCom":
                            ingestedBadge = "<h4><span class='badge badge-info'>FirstPointCom</span></h4>";
                            break;
                        case "BulkVS":
                            ingestedBadge = "<h4><span class='badge badge-primary'>BulkVS</span></h4>";
                            break;
                        case "TeleMessage":
                            ingestedBadge = "<h4><span class='badge badge-warning'>TeleMessage</span></h4>";
                            break;
                    }
                    <tr>
                        <td scope="row">
                            @*<a href="https://acceleratenetworks.com/cart/order/@order.OrderId" class="btn btn-primary">
                                    View Order
                                </a>*@
                            @*<h4 class="d-flex justify-content-between align-items-center mt-3">
                                    <span class="badge badge-info badge-pill">
                                        @number.DateIngested
                                    </span>
                                </h4>*@
                            <h4>@number.DialedNumber</h4>
                        </td>
                        <td>
                            @{
                                if (number.Active)
                                {
                                    <h4><span class="badge badge-success">Active</span></h4>
                                }
                                else
                                {
                                    <h4><span class="badge badge-danger">Inactive</span></h4>
                                }
                            }
                        </td>
                        <td>
                            @Html.Raw(ingestedBadge)
                        </td>
                        <td>
                            <p>@number.Notes</p>
                        </td>
                        <td>
                            <a class="btn btn-danger text-light" href="/Home/OwnedNumbers/@number.DialedNumber">Edit</a>
                            @{
                                if (!string.IsNullOrWhiteSpace(number.BillingClientId))
                                {
                                    <a href="https://billing.acceleratenetworks.com/clients/@number?.BillingClientId/edit" class="btn btn-secondary" target="_blank" rel="noopener noreferrer">
                                        Client
                                    </a>
                                }
                            }
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>